# N5 Pro: Profil ist upstream "experimental"; ohne diese Option bleiben die
# pwm*-Knoten unsichtbar. Der Treiber schreibt beim Laden NICHTS in den EC
# (pwm*_enable startet auf 2 = EC-Automatik). Schreibzugriffe kommen erst
# durch n5-fand.
options minisforum_n5_it5571 experimental_write=1