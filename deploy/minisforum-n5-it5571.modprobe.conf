# Minisforum N5 Pro: the profile is "experimental" upstream; without this option
# the pwm* nodes stay hidden. Loading the driver writes NOTHING to the EC
# (pwm*_enable starts at 2 = EC automatic); only a regulator writes.
options minisforum_n5_it5571 experimental_write=1
